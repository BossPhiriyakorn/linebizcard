# Dependencies
node_modules/
package-lock.json

# Environment variables
.env

# Google Drive / Service Account (ห้าม commit ความลับ)
Credentials.json
**/Credentials.json
**/secrets/*.json
**/*-service-account*.json

# Logs
*.log
logs/
docs

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Build (ไม่ ignore frontend/.next — บิ้วบนเครื่องแล้ว push ขึ้น Git เพื่อไม่ต้อง build บนเซิร์ฟเวอร์)
dist/
build/
# frontend/.next/   ← เปิดให้ commit ผล build ได้
frontend/out/
