-- =============================================================================
-- ลบบัญชีแอดมินจากตาราง admins — ไม่กระทบตาราง users (ลูกค้า)
-- =============================================================================
-- รันใน pgAdmin: Query Tool → แก้รายการอีเมลด้านล่าง (ถ้าต้องการ) → F5
-- หรือ: psql -U postgres -d line_flex_db -f database/delete-admin.sql
-- =============================================================================

BEGIN;

DELETE FROM admins
WHERE email IN (
    'admin@magicbizcard.local'
    -- เพิ่มอีเมลแอดมินอื่นที่ต้องการลบได้ที่นี่
);

COMMIT;
